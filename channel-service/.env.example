# Port the simulated channel service Express app binds to
PORT=4000

# Endpoint of the CRM webhook receipt endpoint to post delivery status callbacks
CRM_CALLBACK_URL=http://localhost:3000/api/receipt
